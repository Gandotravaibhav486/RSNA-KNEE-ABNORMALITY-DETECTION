# docs/

`method.html` — the shareable method note: the 58-label constraint, the three instruments and
their 2σ bars, the experiment lifecycle, the verdict scheme, results to date, and the failure modes
we have already paid for.

Published at https://claude.ai/artifact/DmF3qq7toLBU25CaRsSUVj (private until shared).
Republish by passing that URL, so the link stays stable.

`kaggle-setup-prompt.md` / `setup-prompt.html` — the reusable competition-start prompt, distilled
from this run. Published at https://claude.ai/artifact/3g4gJKccnUiu17kTpTx8Fi

Source of truth remains the repo: `AGENTS.md` (rules), `rules.md` (competition constraints),
`experiments.md` (ledger), `agent-log.md` (what actually happened). The note summarises those —
when they disagree, they win.
